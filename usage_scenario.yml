name: "YouTube Free" # Change this to "YouTube AdBlock" or "YouTube Premium" as appropriate
author: "Arturo"
description: Measuring youtube free version
version: 1

services:
  browser-client:
    image: prueba-youtube
    build:
      context: .
    command: sleep infinity

flow:
  - name: PlayVideoÒ
    container: browser-client
    commands:
      - type: console
        command: python measure_ytb_noAddBlock.py
        note: "laying YouTube video"
